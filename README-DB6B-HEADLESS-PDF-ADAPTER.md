# DB-6B — Headless PDF Adapter (superseded by PDF + DOCX generation patch)

The original server-safe PDF adapter remains supported. The newer DB-6B package also adds a headless editable DOCX adapter through `HeadlessDocumentGenerationService`.

See `README-DB6B-PDF-DOCX-GENERATION.md` for the current behavior and verification status.
