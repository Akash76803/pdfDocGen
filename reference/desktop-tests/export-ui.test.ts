import{describe,expect,it}from'vitest';import{buildExportRequest,DEFAULT_EXPORT_UI_STATE,validateExportUi,visibleExportOptions}from'../src/utils/exportUi.js';
const context={activeDocumentId:'a',selectedDocumentIds:['a','b'],pageCount:3,pageWidthMm:210,pageHeightMm:297};describe('Phase 4.19.3 export UI logic',()=>{it('shows only format-specific controls and disables DOCX',()=>{expect(visibleExportOptions('PDF')).toMatchObject({pdf:true,raster:false});expect(visibleExportOptions('PNG')).toMatchObject({png:true,raster:true});expect(visibleExportOptions('JPEG')).toMatchObject({jpeg:true,raster:true});expect(visibleExportOptions('DOCX').docxPending).toBe(true);});it('constructs PNG and JPEG orchestrator requests with the active page',()=>{expect(buildExportRequest({...DEFAULT_EXPORT_UI_STATE,format:'PNG',pageSelection:'CURRENT',currentPageIndex:1},'t','a',[])).toMatchObject({format:'PNG',documentGroupIds:['a'],options:{png:{dpi:300,pageSelection:'CURRENT',currentPageIndex:1}}});expect(buildExportRequest({...DEFAULT_EXPORT_UI_STATE,format:'JPEG',scope:'SELECTED'},'t','a',['a','b'])).toMatchObject({format:'JPEG',documentGroupIds:['a','b'],options:{jpeg:{quality:90,backgroundColor:'#FFFFFF'}}});});it('validates filename, scope, quality, background, and pending DOCX',()=>{expect(validateExportUi(DEFAULT_EXPORT_UI_STATE,context)).toEqual([]);expect(validateExportUi({...DEFAULT_EXPORT_UI_STATE,format:'JPEG',jpegQuality:30,jpegBackground:'white',scope:'SELECTED'}, {...context,selectedDocumentIds:[]})).toHaveLength(3);expect(validateExportUi({...DEFAULT_EXPORT_UI_STATE,format:'DOCX'},context)[0]).toMatch(/DOCX/);});});
