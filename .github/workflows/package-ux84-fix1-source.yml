name: Package UX8.4 Fix1 ZIP

on:
  push:
    branches:
      - feat/ux8-conditional-rendering

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Package source
        run: |
          zip -qr Document-Builder-UX8.4-Fix1-Gap-Stability.zip . -x ".git/*" ".github/workflows/package-ux84-fix1-source.yml"
      - uses: actions/upload-artifact@v4
        with:
          name: Document-Builder-UX8.4-Fix1-Gap-Stability
          path: Document-Builder-UX8.4-Fix1-Gap-Stability.zip
          if-no-files-found: error
